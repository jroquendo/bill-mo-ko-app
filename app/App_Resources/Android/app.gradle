// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.bill.moko.app"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
